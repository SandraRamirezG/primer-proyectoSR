import { Icon } from "react-bootstrap-icons";

const CartWidget = () => {
    return (
        <div>
            <Icon.Cart />
            <div>
                <span>4</span>

            </div>

        </div>

    );
};

export default CartWidget;